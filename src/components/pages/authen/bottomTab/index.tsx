import React, { memo } from "react";
import BottomTabTemplates from "../../../templates/bottomTab";

const BottomTabPages = () => {
  return <BottomTabTemplates />;
};

export const BottomTab = memo(BottomTabPages);
